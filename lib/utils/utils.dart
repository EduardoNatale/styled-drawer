import 'package:flutter/material.dart';

class Utils {
  static Duration longAnimationDuration = Duration(milliseconds: 1000);
  static Duration mediumAnimationDuration = Duration(milliseconds: 600);
  static Duration shortAnimationDuration = Duration(milliseconds: 300);
  static double drawerHorizontalPadding = 32;
  static double patternPadding = 16;
  static double drawerItemHeight = 56;
  static double spaceBetween = 12;
  static double patternRadius = 8;
  static double drawerRadius = 20;
  static double appBarHeight = 56;
  static Curve curves = Curves.fastOutSlowIn;
  static double width;
  static TextStyle boldText = TextStyle(fontWeight: FontWeight.bold);
}
